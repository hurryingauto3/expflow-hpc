# Core dependencies
pyyaml>=6.0
pandas>=1.3.0

# Optional: AI suggestions (Gemini API)
google-generativeai>=0.3.0

# Optional: Remote database backends (ExpFlow v0.8.0+)
# Uncomment to enable MongoDB support:
pymongo>=4.0.0

# Uncomment to enable PostgreSQL support:
# psycopg2-binary>=2.9.0

# To install with MongoDB: pip install -r requirements.txt pymongo
# To install with PostgreSQL: pip install -r requirements.txt psycopg2-binary
# To install both: pip install -r requirements.txt pymongo psycopg2-binary

