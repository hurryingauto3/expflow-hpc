# Core dependencies
pyyaml>=6.0
pandas>=1.3.0

# Optional: AI suggestions (Gemini API)
google-generativeai>=0.3.0

# Optional: Remote database backends (ExpFlow v0.8.0+)
# MongoDB support (native in BaseExperimentManager)
pymongo>=4.0.0

# PostgreSQL support (native in BaseExperimentManager)
# psycopg2-binary>=2.9.0

# To install with MongoDB: pip install -r requirements.txt
# To install with PostgreSQL: pip install -r requirements.txt psycopg2-binary
# To install both: pip install -r requirements.txt psycopg2-binary

