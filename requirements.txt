# NOTE: prefer ``pip install expflow`` (or ``pip install -e .`` from
# source). pyproject.toml is the canonical dependency source. This
# requirements.txt is kept for users who want a flat list of every
# extra rather than picking individual ``[extras]``.

# Core dependency (required)
pyyaml>=6.0

# Optional extras — uncomment as needed, or use ``pip install expflow[<name>]``.
#
# pandas         → analysis  : CSV export, results harvester, analysis pipeline
# pymongo        → mongodb   : MongoDB results backend
# psycopg2-binary → postgresql: PostgreSQL results backend
# google-generativeai → ai   : Gemini-based suggestions

pandas>=1.3.0
pymongo>=4.0.0
# psycopg2-binary>=2.9.0
# google-generativeai>=0.3.0
